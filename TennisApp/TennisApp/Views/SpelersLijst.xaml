<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TennisApp.Views.SpelersLijst"
             Title="{Binding Title}">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add Player" Command="{Binding AddPlayerCommand}" />
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <StackLayout Padding="20">
            <Label Text="SpelersLijst"></Label>
            <CollectionView ItemsSource="{Binding Player}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout>
                            <Label Text="{Binding PlayerId}"/>
                            <StackLayout Orientation="Horizontal">
                                <Label Text="{Binding Firstname}"/>
                                <Label Text="{Binding Lastname}"/>
                            </StackLayout>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            <Button WidthRequest="54" Text="Add Player" BackgroundColor="LightGreen" Command="{Binding AddPlayerCommand}"/>
            <Button WidthRequest="54" Text="Terug" BackgroundColor="LightGreen" Command="{Binding TerugPageCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>